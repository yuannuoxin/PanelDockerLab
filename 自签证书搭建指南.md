# 自建 CA 自签证书完整指南（根证书 + 域名证书）

> 从零搭建「根证书 + 域名证书」体系的手记：覆盖生成根 CA、签发域名证书、安装系统信任、平台配置与实战排错。
> 适用于内网 HTTPS、本地开发、KPanel / Nginx 反代、Cloudflare Custom Hostname 等场景。



***

## 1. 为什么需要自签证书

内网服务、开发环境、面板反代等场景需要 TLS 证书，但不方便使用公共 CA 时，自建 CA 是标准做法：



* **根证书（Root CA）**：自己签发的 "信任锚"。只需导入系统一次，以后所有由它签发的证书都会被信任。

* **域名证书（Leaf）**：实际给站点使用的证书，由根证书签发，带 SAN（浏览器校验域名）。

## 2. 目录结构



```
myca/

├── certs/

│   ├── root.crt        # 根证书（导入系统信任，只需一次）

│   ├── server.crt      # 域名证书（站点使用）

│   └── fullchain.crt   # 叶子+根 拼接（上传证书链用）

├── private/

│   ├── root.key        # 根私钥（永久保存，勿泄露）

│   └── server.key      # 域名私钥

├── root.cnf            # 根证书配置文件

├── leaf\_ext.conf       # 域名证书扩展配置

└── server.csr          # 证书签名请求（签发后即可忽略）
```

## 3. 生成根证书（只执行一次）

### 3.1 配置文件 `root.cnf`



```
\[req]

distinguished\_name = dn

x509\_extensions = v3\_ca

prompt = no

\[dn]

C=CN

ST=Sichuan

L=Nanchong

O=MyEC RootCA

CN=MyEC Root CA

\[v3\_ca]

basicConstraints = critical,CA:TRUE

keyUsage = critical,keyCertSign, cRLSign

subjectKeyIdentifier = hash
```

### 3.2 生成根私钥与根证书（10 年）



```
\# EC P-256 根私钥（也可用 RSA 2048，但文件大 5\~7 倍）

openssl genpkey -algorithm EC -pkeyopt ec\_paramgen\_curve:prime256v1 -out private\root.key

\# 自签根证书，10 年有效期

openssl req -x509 -new -key private\root.key -sha256 -days 3650 -out certs\root.crt -config root.cnf
```

> ⚠️ 必须用 
>
> `-config root.cnf`
>
>  指定扩展，不要用 
>
> `openssl req -x509 -addext basicConstraints=...`
>
> （会产生重复扩展，证书被判无效，见 §9.2）。

## 4. 签发域名证书（每个新域名执行一次）

### 4.1 扩展配置 `leaf_ext.conf`



```
subjectAltName = DNS:kpanel.003.mtt.qzz.io,IP:127.0.0.1

basicConstraints = critical,CA:FALSE

keyUsage = digitalSignature, keyEncipherment

extendedKeyUsage = serverAuth

subjectKeyIdentifier = hash

authorityKeyIdentifier = keyid,issuer
```

### 4.2 生成私钥、CSR 并签发



```
\# 域名私钥

openssl genpkey -algorithm EC -pkeyopt ec\_paramgen\_curve:prime256v1 -out private\server.key

\# CSR（域名写在这里）

openssl req -new -key private\server.key -out server.csr -subj "/C=CN/ST=Sichuan/L=Nanchong/O=MyEC Site/CN=kpanel.003.mtt.qzz.io"

\# 用根证书签发（关键：-extfile 指定 SAN 等扩展）

openssl x509 -req -in server.csr -CA certs\root.crt -CAkey private\root.key -CAcreateserial -days 365 -sha256 -extfile leaf\_ext.conf -out certs\server.crt

\# 拼接 fullchain（叶子 + 根，供面板/服务端使用）

Get-Content certs\server.crt | Out-File -Encoding ascii certs\fullchain.crt

Get-Content certs\root.crt | Add-Content -Encoding ascii certs\fullchain.crt
```

> 现代浏览器只认 SAN 不认 CN，SAN 必须与访问域名完全一致（多域名用逗号分隔，如 
>
> `DNS:a.example.com,DNS:b.example.com,IP:192.168.1.10`
>
> ）。

## 5. 安装根证书到系统信任

### Windows



```
\# 当前用户（推荐，无需管理员）

certutil -user -addstore Root certs\root.crt

\# 全机器所有用户（需管理员权限）

certutil -addstore -f Root certs\root.crt
```

或双击 `root.crt` → 安装证书 → **本地计算机** → 选择「**受信任的根证书颁发机构**」。

> ⚠️ 必须装进「受信任的根证书颁发机构」。装到「中间证书颁发机构」或「个人」均无效（浏览器不认）。安装后
>
> **必须重启浏览器**
>
> 。

### macOS



```
sudo security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain root.crt
```

### Linux（Debian / Ubuntu）



```
sudo cp root.crt /usr/local/share/ca-certificates/

sudo update-ca-certificates
```

## 6. 平台配置

### KPanel / 类似管理面板



| 字段         | 填写                    |
| ---------- | --------------------- |
| 公钥证书链（PEM） | `fullchain.crt` 的全部内容 |
| 私钥（PEM）    | `server.key` 的全部内容    |

### Nginx



```
ssl\_certificate     /path/to/fullchain.crt;

ssl\_certificate\_key /path/to/server.key;
```

## 7. 验证



```
\# 1. 证书链验证（OK = 叶子由根签发）

openssl verify -CAfile certs\root.crt certs\server.crt

\# 2. 查看 SAN（必须包含访问域名）

openssl x509 -in certs\server.crt -noout -ext subjectAltName

\# 3. 密钥配对（两个 pubkey 文件应完全一致）

openssl x509 -in certs\server.crt -noout -pubkey > p1.pem

openssl pkey -in private\server.key -pubout > p2.pem

\# 4. 线上证书验证（Verify return code: 0 (ok) 即成功）

openssl s\_client -connect kpanel.003.mtt.qzz.io:443 -servername kpanel.003.mtt.qzz.io -CAfile certs\root.crt
```

## 8. 后续签发新域名（复用根 CA，不再重建）

只需修改域名（§4.1 的 SAN 和 §4.2 的 CN），重复第 4 步即可：



```
\$domain = "new.example.com"

openssl genpkey -algorithm EC -pkeyopt ec\_paramgen\_curve:prime256v1 -out private\server.key

openssl req -new -key private\server.key -out server.csr -subj "/CN=\$domain"

\# 修改 leaf\_ext.conf 的 subjectAltName 后：

openssl x509 -req -in server.csr -CA certs\root.crt -CAkey private\root.key -CAcreateserial -days 365 -sha256 -extfile leaf\_ext.conf -out certs\server.crt
```

根证书 `root.crt` / `root.key` 永远不动。

## 9. 清理与移除

### 删除本地证书文件



```
\# 删除证书与私钥目录、CSR（配置文件 root.cnf / leaf\_ext.conf 可保留，方便下次重建）

Remove-Item C:\tmp\myca\certs -Recurse -Force

Remove-Item C:\tmp\myca\private -Recurse -Force

Remove-Item C:\tmp\myca\server.csr
```

### 从系统移除信任的根证书（可选，谨慎）



```
\# 查看当前已安装的自签根证书

Get-ChildItem Cert:\CurrentUser\Root | Where-Object { $\_.Subject -like "\*MyEC\*" }

\# 删除当前用户的（无需管理员）

Get-ChildItem Cert:\CurrentUser\Root | Where-Object { $\_.Subject -like "\*MyEC\*" } | Remove-Item

\# 删除全机器的（需管理员）

Get-ChildItem Cert:\LocalMachine\Root | Where-Object { $\_.Subject -like "\*MyEC\*" } | Remove-Item
```

> ⚠️ 移除根证书后，所有由该根签发的站点证书会重新变为「不受信任」，请确认不再需要再执行。

## 10. 实战排错记录

### 9.1 浏览器仍显示 "不安全"



* 根证书装错位置（在「中间证书颁发机构」而不是「受信任的根证书颁发机构」）→ 按 §5 重装，并重启浏览器

* 证书 SAN 与访问域名不一致 → 重新签发并修正 SAN

* 服务器只发了叶子证书而客户端无法补全链 → 上传 `fullchain.crt`

### 9.2 `certificate PEM is invalid`



* 根因：`openssl req -x509` 配合 `-addext basicConstraints=...` 时与 OpenSSL 默认配置叠加，产生**两个冲突的 Basic Constraints（CA:TRUE + CA:FALSE）**，证书无效

* 排查：`openssl x509 -in cert.crt -noout -text | findstr "CA:"`，正常应只有一行

* 解决：改用 `-config root.cnf` / `-extfile leaf_ext.conf` 配置文件方式签发

### 9.3 Git Bash 下 `-subj "/C=CN/..."` 报错



* Git Bash 会把 `/C=CN` 当作路径做转换（变成 `C:/Program Files/Git/C=CN`）

* 解决：执行前 `export MSYS_NO_PATHCONV=1`，或改用 PowerShell / CMD，或交互式输入 DN

### 9.4 私钥报 `invalid or encrypted`



* 私钥被加密（生成时设置了密码）

* 解决：用 `openssl genpkey` 生成默认未加密私钥，不要用交互式设密码的生成方式

### 9.5 面板报 `site writer unavailable`



* 面板（如 KPanel）后端写证书的服务异常，与证书内容无关

* 排查：面板服务状态、磁盘空间、服务日志，重启面板服务后重试

## 11. 安全提醒



* `root.key` 是信任锚的命根子：一旦泄露，所有由它签发的证书全部失效。务必离线备份、严格权限。

* 私钥和根私钥**不要提交到 GitHub**，仓库中建议 `.gitignore`：



```
\*.key
```



* 自签证书只适用于内网 / 开发 / 测试环境；公网生产环境请使用 Let's Encrypt 或商业 CA。